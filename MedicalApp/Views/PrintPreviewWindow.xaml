<Window x:Class="MedicalApp.Views.PrintPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Print Preview" Height="750" Width="620"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="#F1F5F9"
        ResizeMode="CanResize">
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border BorderBrush="#CBD5E1" BorderThickness="1.5" CornerRadius="8" Background="White">
            <FlowDocumentPageViewer Name="DocViewer" Zoom="90"/>
        </Border>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Style="{StaticResource ModernButton}" Content="Print..." Click="Print_Click" Width="110" Height="38" Margin="0,0,10,0"/>
            <Button Style="{StaticResource ModernButton}" Content="Close" Click="Close_Click" Background="#EF4444" BorderBrush="#EF4444" Width="100" Height="38"/>
        </StackPanel>
    </Grid>
</Window>
